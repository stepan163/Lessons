#include<std_lib.h>

int main()
{
    vector<double> distances;

    cout << "Enter distances: ";
    for (double distance; cin >> distance;)
        distances.push_back(distance);

    // [ 610.5, 204.6, 300.0, ...]
    double max {distances[0plit };
    f
//    for (int distance : distances)
//        cout << distance << ' ';
//    cout << '\n';

    return 0;

}
